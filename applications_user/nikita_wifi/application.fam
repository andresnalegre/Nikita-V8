App(
    appid="nikita_wifi",
    name="WIFI",
    apptype=FlipperAppType.MENUEXTERNAL,
    entry_point="nikita_wifi_app",
    requires=["gui"],
    stack_size=4 * 1024,
    icon="A_WIFI_14",
    order=45,
    fap_version="1.1",
    fap_icon="icons/wifi_10px.png",
    fap_category="GPIO",
    fap_description="Nikita's WiFi. Opens the full ESP32 Marauder if installed, else drives the board over the GPIO UART: scan/sniff/deauth/beacon/channel. Authorised testing only.",
    fap_author="Nikita",
    sources=["nikita_wifi.c"],
)
